// lib/instagram.js
// Llamadas a la Graph API de Meta para enviar mensajes por Instagram DM.
// Implementación completa en el paso 5 del plan.

export async function enviarMensaje(recipientId, texto) {
  // TODO (paso 5): POST a https://graph.facebook.com/v19.0/me/messages
  //   con { recipient, message, messaging_type: 'RESPONSE', access_token }.
  throw new Error('enviarMensaje aún no implementado')
}
