// lib/sheets.js
// Guarda leads en Google Sheets usando la librería googleapis.
// Implementación completa en el paso 6 del plan.

export async function guardarLead({ senderId, mensaje, respuesta }) {
  // TODO (paso 6): autenticar con JWT (service account) y hacer append en
  // el rango 'Leads!A:E' del spreadsheet indicado en GOOGLE_SHEETS_ID.
  throw new Error('guardarLead aún no implementado')
}
