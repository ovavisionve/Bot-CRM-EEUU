// lib/respuestas.js
// Motor de palabras clave — lee config/respuestas.json.
// Implementación completa en el paso 4 del plan.

export function detectarRespuesta(mensaje) {
  // TODO (paso 4): normalizar el texto (NFD + quitar acentos) y matchear
  // contra config.palabras_clave; si no hay coincidencia devolver respuesta_default.
  throw new Error('detectarRespuesta aún no implementado')
}
