export const metadata = {
  title: 'OVA VISION — Bot Instagram',
  description: 'Bot de respuestas automáticas de Instagram de OVA VISION.',
}

export default function RootLayout({ children }) {
  return (
    <html lang="es">
      <body>{children}</body>
    </html>
  )
}
