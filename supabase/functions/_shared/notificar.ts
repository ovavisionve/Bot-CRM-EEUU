// Notifica al admin cuando llega un mensaje nuevo.
// Implementación completa en el paso 7.

export async function notificarAdmin(data: {
  senderId: string
  mensaje: string
}) {
  // TODO (paso 7): enviar notificación por email o WhatsApp link
  throw new Error("notificarAdmin aún no implementado")
}
