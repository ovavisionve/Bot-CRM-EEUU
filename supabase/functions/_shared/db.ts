// Guarda leads en Supabase Postgres.
// Implementación completa en el paso 6.

export async function guardarLead(data: {
  senderId: string
  mensaje: string
  respuesta: string
}) {
  // TODO (paso 6): insertar en tabla "leads" usando Supabase client
  throw new Error("guardarLead aún no implementado")
}
