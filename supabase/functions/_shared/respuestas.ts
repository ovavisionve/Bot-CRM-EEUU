// Motor de palabras clave — lee respuestas.json
// Implementación completa en el paso 4.

import config from "./respuestas.json" with { type: "json" }

export function detectarRespuesta(mensaje: string): string {
  // TODO (paso 4): normalizar texto y matchear contra palabras clave
  throw new Error("detectarRespuesta aún no implementado")
}
